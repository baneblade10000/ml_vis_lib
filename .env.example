# Copy to .env and fill in values
WEB_PORT=8080

# Cloudflare Tunnel token (from Zero Trust → Networks → Tunnels → Configure)
# Public hostname in tunnel config should point to http://web:80
TUNNEL_TOKEN=
