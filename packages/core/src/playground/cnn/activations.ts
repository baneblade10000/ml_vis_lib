/** Activation ids for the CNN playground UI / WASM config. */

export type CnnActivationId = "relu" | "tanh" | "sigmoid" | "linear";

export const CNN_ACTIVATION_IDS: CnnActivationId[] = ["relu", "tanh", "sigmoid", "linear"];
